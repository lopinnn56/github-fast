{
    "name": "github-fast",
    "version": "2.1.0",
    "private": true,
    "type": "module",
    "description": "纯前端 GitHub 加速工具（静态站点，esbuild 构建）",
    "scripts": {
        "dev": "node scripts/build.mjs --watch",
        "build": "node scripts/build.mjs",
        "lint": "eslint .",
        "preview": "npm run build && npx -y serve dist",
        "test": "node scripts/test.convert.mjs"
    },
    "devDependencies": {
        "@eslint/js": "^9.17.0",
        "esbuild": "^0.25.0",
        "eslint": "^9.17.0",
        "globals": "^15.14.0"
    }
}
